services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - "80"
    networks:
      proxy:
        aliases:
          - lazcanocloud-web
networks:
  proxy:
    external: true
    name: mus_proxy